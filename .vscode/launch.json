{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "firefox",
            "type": "firefox",
            "request": "launch",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceRoot}/src"
        }
    ]
}